#include<stdio.h>
#include<math.h>
#include<stdlib.h>

int genRandInterval(float l)
{
	//srand(time(NULL));
	double u = ((double)rand()/(double)RAND_MAX);
	float t = -1/l * log(1 - u);
	return (int)(t+1);
}


